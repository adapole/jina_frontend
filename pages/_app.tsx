import '../styles/globals.css';
import App from './app';

function MyApp({ Component, pageProps }) {
	return (
		<>
			<App />
			{/* <Component {...pageProps} /> */}
		</>
	);
}

export default MyApp;
